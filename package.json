{
  "name": "kamalafarms-tech-pms",
  "version": "1.0.0",
  "description": "Kamalafarms Tech - Performance Management System",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "setup-db": "cd server && node scripts/setupDb.js",
    "build": "cd client && npm run build",
    "render:build": "cd server && npm install && cd ../client && npm install && npm run build",
    "render:start": "cd server && node scripts/setupDb.js && NODE_ENV=production node index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
